heroku login

heroku git:remote -a inspireme2

git remote -v

git push heroku master

heroku logs --tail

*** Undoing commits:
git reset HEAD~

*** Changing back the code:
git checkout <commit hash> <filename>